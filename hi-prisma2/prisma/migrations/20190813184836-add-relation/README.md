# Migration `20190813184836-add-relation`

This migration has been generated by haoxin at 8/13/2019, 6:48:36 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User"("id" text NOT NULL  ,"name" text   ,"email" text NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE TABLE "public"."Post"("id" text NOT NULL  ,"title" text NOT NULL DEFAULT '' ,"content" text   ,"published" boolean NOT NULL DEFAULT false ,"createdAt" timestamp(3) NOT NULL  ,"updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY ("id"));

ALTER TABLE "public"."Post" ADD COLUMN "author" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190729195514-init..20190813184836-add-relation
--- datamodel.dml
+++ datamodel.dml
@@ -10,11 +10,16 @@
 model User {
   id    String  @id @default(cuid())
   name  String?
   email String  @unique
+  posts Post[]
 }
 model Post {
   id        String  @id @default(cuid())
   title     String
+  content   String?
+  author    User?
   published Boolean @default(false)
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190813184836-add-relation)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190813184836-add-relation'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
