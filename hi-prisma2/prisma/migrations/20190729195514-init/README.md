# Migration `20190729195514-init`

This migration has been generated by haoxin at 7/29/2019, 7:55:14 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User"("id" text NOT NULL  ,"name" text   ,"email" text NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE TABLE "public"."Post"("id" text NOT NULL  ,"title" text NOT NULL DEFAULT '' ,"published" boolean NOT NULL DEFAULT false ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "User.email._UNIQUE" ON "public"."User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190729195514-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,20 @@
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://prisma2:prisma2@localhost:8300/prisma2?schema=public"
+}
+
+generator photonjs {
+  provider = "photonjs"
+}
+
+model User {
+  id    String  @id @default(cuid())
+  name  String?
+  email String  @unique
+}
+
+model Post {
+  id        String  @id @default(cuid())
+  title     String
+  published Boolean @default(false)
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190729195514-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190729195514-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
